<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Wakkey" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/peach_background</item>
        <item name="colorPrimary">@color/black</item>
        <item name="colorButtonNormal">@android:color/transparent</item>
        <item name="android:buttonStyle">@drawable/border_button</item>
    </style>

    <style name="Theme.Wakkey" parent="Base.Theme.Wakkey" />
</resources>